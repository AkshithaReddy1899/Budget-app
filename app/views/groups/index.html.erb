<div class="container text-center">
  <div class="row">
	  <div class="col-sd-12 col-lg-6border">
			<% if @groups.blank? %>
				<p>No categories created yet.</p>
			<%else%>
			  <p>Not empty!</p>
			<%end%>
		</div>
	</div>
	<hr>
  <div class="bottom-btn">
  <%= link_to "Add a new category", new_group_path, class: "btn btn-secondary" %>
	</div>
</div>